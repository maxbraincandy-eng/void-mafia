const express=require('express'); const {ROLES,DEFAULT_SETTINGS,publicRoom}=require('../services/gameEngine'); const {leaderboard,findUserById}=require('../services/userService'); const {createClan,listClans,joinClan}=require('../services/clanService'); function createApiRouter(ctx){const r=express.Router(); r.get('/webrtc',(req,res)=>res.json({ok:true,iceServers:ctx.webrtc.iceServers})); r.get('/roles',(req,res)=>res.json({ok:true,roles:ROLES,defaultSettings:DEFAULT_SETTINGS})); r.get('/rooms',(req,res)=>res.json({ok:true,rooms:[...ctx.rooms.values()].map(x=>({id:x.id,name:x.name,phase:x.phase,hostName:x.hostName,players:x.players.length,maxPlayers:x.settings.maxPlayers,language:x.settings.language}))})); r.get('/rooms/:id',(req,res)=>{const room=ctx.rooms.get(String(req.params.id)); if(!room)return res.status(404).json({ok:false,error:'Room not found'}); res.json({ok:true,room:publicRoom(room,Number(req.query.userId||0))})}); r.get('/leaderboard',async(req,res)=>res.json({ok:true,users:await leaderboard(ctx)})); r.get('/users/:id',async(req,res)=>{const u=await findUserById(ctx,req.params.id); if(!u)return res.status(404).json({ok:false,error:'User not found'}); res.json({ok:true,user:u})}); r.get('/clans',async(req,res)=>res.json({ok:true,clans:await listClans(ctx)})); r.post('/clans',async(req,res)=>{try{res.json({ok:true,clan:await createClan(ctx,{...req.body,owner:req.body.user})})}catch(e){res.status(400).json({ok:false,error:e.message})}}); r.post('/clans/:id/join',async(req,res)=>{try{res.json({ok:true,clan:await joinClan(ctx,req.params.id,req.body.user)})}catch(e){res.status(400).json({ok:false,error:e.message})}}); r.get('/store',(req,res)=>res.json({ok:true,items:[{key:'walkieTalkie',title:'Mafia Walkie-Talkie',price:0,description:'Mafia private night channel'},{key:'roomNames',title:'Room Names',price:0,description:'Custom room names'},{key:'blueMagicBall',title:'Blue Magic Ball',price:300,description:'Extra reveal ability'},{key:'avatarFrameNeon',title:'Neon Avatar Frame',price:150,description:'Profile frame'}]})); return r} module.exports={createApiRouter};
