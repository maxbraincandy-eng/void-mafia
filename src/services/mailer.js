const nodemailer=require('nodemailer'); function createMailer(){if(!process.env.SMTP_HOST||!process.env.SMTP_USER||!process.env.SMTP_PASS)return null; return nodemailer.createTransport({host:process.env.SMTP_HOST,port:Number(process.env.SMTP_PORT||587),secure:Number(process.env.SMTP_PORT||587)===465,auth:{user:process.env.SMTP_USER,pass:process.env.SMTP_PASS}})} async function sendEmailCode(ctx,email,code){if(!ctx.mailer){console.log('DEV EMAIL CODE',email,code);return} await ctx.mailer.sendMail({from:process.env.MAIL_FROM||process.env.SMTP_USER,to:email,subject:'VOID MAFIA code',text:`Your VOID MAFIA code is: ${code}`})} module.exports={createMailer,sendEmailCode};
