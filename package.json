{
  "name": "void-mafia",
  "version": "2.0.0",
  "private": true,
  "description": "Void Mafia — cyberpunk social deduction game",
  "scripts": {
    "dev": "concurrently -n server,client -c cyan,magenta \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w server && npm run build -w client",
    "start": "NODE_ENV=production node server/dist/index.js",
    "install:all": "npm install && npm install -w server && npm install -w client",
    "typecheck": "npm run typecheck -w server && npm run typecheck -w client"
  },
  "workspaces": [
    "server",
    "client"
  ],
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18"
  }
}
