[build]
buildCommand = "npm install --include=dev && npm run build"

[deploy]
startCommand = "npx --yes serve dist -l $PORT -s"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
